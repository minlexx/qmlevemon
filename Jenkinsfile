pipeline {
    agent { label 'windows qt5' }

    stages {
        stage('Build') {
            steps {
                echo 'Building..'
                bat ''' echo Lol '''
            }
        }
    }
}
